fastapi[standard]
sqlmodel
uvicorn
psycopg2-binary
httpx


python-dotenv==1.0.1      # For reading environment variables stored in .env file
langchain==0.2.2
langchain-community==0.2.3
langchain-openai==0.1.8   # For embeddings
unstructured==0.18.3      # Document loading
onnxruntime==1.22.0       # chromadb dependency: install via pip on Ubuntu
chromadb==1.0.15       # Vector storage
openai==1.31.1            # For embeddings
tiktoken==0.9.0           # For embeddings

# install markdown dependencies with: pip install "unstructured[md]" after installing the requirements file. Leave this line commented out.